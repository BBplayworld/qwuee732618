{
    "build": {
        "env": {
            "NODE_ENV": "production"
        }
    },
    "devCommand": "nuxt dev",
    "buildCommand": "nuxt build",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "preview": {
        "buildCommand": "nuxt build",
        "devCommand": "nuxt preview"
    }
}